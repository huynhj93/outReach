//1. Declare a function called 'hello' that console logs 'Hello World!'

var hello = 'FILL ME IN';
	
//2. Declare a function called 'helloPerson' that takes in 'person' as an argument and console logs 
// 	 'Hello ' + ____  where blank represents that 'person';

var helloPerson = 'Fill ME IN';

//3. Declare a function called 'addTen' that takes in a number and then outputs that number + 10;

var addTen = 'Fill ME IN';

//4. Declare a function called 'square' that takes in a number and then outputs that number squared;
//	 (for example, if the input was 3, the output would be 9, because 3*3 = 9).

var square = 'Fill ME IN';

//5. Find the value of 481943 squared, using the function you declared in problem 4. 

var n = 'FILL_ME_IN';

//5. Declare a function called 'product' that takes in two numbers and then outputs the product of those
//   2 numbers;

var product = 'Fill ME IN';

//6. Find the product of 123 * 456 using the function you declared in problem 5.

var o = 'Fill ME IN'; // o should be the product of 123 * 456;

